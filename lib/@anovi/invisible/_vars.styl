/*
* > Grid
*/
if $grid-columns is not defined
  $grid-columns = 12

if $grid-gutter-width is not defined
  $grid-gutter-width = 30px

if $grid-breakpoints is not defined
  $grid-breakpoints = {
    // Extra small screen / phone
    xs: 0,
    // Small screen / phone
    sm: 576px,
    // Medium screen / tablet
    md: 768px,
    // Large screen / desktop
    lg: 992px,
    // Extra large screen / wide desktop
    xl: 1200px
  }

if $container-max-widths is not defined
  $container-max-widths = {
    sm: 540px,
    md: 720px,
    lg: 960px,
    xl: 1140px
  }


/*
* > Sizes
*/
if $sizes is not defined
  $sizes = {
    '25': 25%,
    '50': 50%,
    '75': 75%,
    '100': 100%,
    auto: auto
  }


/*
* > Typography and Vertical rythm
*/
if $grid-unit-lines is not defined
  $grid-unit-lines = 2

gu($unit = 1)
  return s('calc(var(--grid-unit) * %s)', $unit)


/*
* > Spacers
*/
if $spacers is not defined
  $spacers = {
    '0': 0,
    '01':   gu(1),
    '1':    gu(s('var(--grid-unit-lines) * 1')),
    '2':    gu(s('var(--grid-unit-lines) * 2')),
    '3':    gu(s('var(--grid-unit-lines) * 3')),
    '4':    gu(s('var(--grid-unit-lines) * 4')),
    'auto': s('auto')
  }


/*
* > Debugging
*/
if $show-grid is not defined
  $show-grid = false
